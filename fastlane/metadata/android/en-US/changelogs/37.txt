Bring your own models: import custom transcription models from a folder or link (Transducer, Whisper fine-tunes, CTC, SenseVoice; integrity verified, split files supported, dedicated share target each). Model tab redesigned: clearer Parakeet/Whisper variant selection, honest speed/quality comparison across all 8 engines. NNAPI selectable on every device with automatic CPU recovery after crashes. Fixed: Gemma backend selection crash (broke 1.9.2 on F-Droid); quantized Parakeet variant failing to load on some devices. Engines unified: smaller app, faster updates.